enum AppBarChoise { none, generators, waveForms }

enum Generator {
  pickUp,
  laser,
  explosion,
  powerUp,
  hit,
  blip,
  synth,
  random,
  tone,
  mutate,
  none,
}

enum WaveForm {
  square,
  triangle,
  sine,
  sawtoothRising,
  sawtoothFalling,
  whiteNoise,
  pinkNoise,
  redNoise,
  none,
}

enum SampleRate {
  kHz44, // 44 KHz
  kHz22,
  kHz11,
  kHz55, // 5.5 KHz
}
